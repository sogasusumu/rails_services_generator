Description:
   repositoryを生成します。repositoryは、必ず、コントローラーアクションで利用するモデルと1対1の関係で作成してください。
   対になるコントローラーアクションで利用するモデルが存在しない場合、例外を返却します。

Example:
    rails generate repository things#index-model_name

    This will create:
        app/things/index/model_name_repository.rb
