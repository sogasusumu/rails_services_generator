Description:
   interactorを生成します。interactorは、必ず、コントローラーアクションと1対1の関係で作成してください。
   対になるコントローラーアクションが存在しない場合、例外が発生します。

Example:
    # ThingController#indexに対応する場合
    rails generate interactor Thing#index

    This will create:
        app/interactors/things/index_interactor.rb
